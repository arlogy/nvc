{
  "name": "nvc",
  "description": "Network View Controller",
  "scripts": {
    "test": "echo Running tests... && npm run test:core -s && npm run test:fsm -s && npm run test:quick -s && npm run test:zexpl -s && echo Finished!",
    "test:core": "mocha ./tests/nvc.js",
    "test:fsm": "mocha ./tests/nvc_fsm.js",
    "test:quick": "mocha ./tests/nvc_quick.js",
    "test:zexpl": "mocha ./tests/nvc_examples.js",
    "examples:about": "echo Examples-related tests will fail when unexpected data paths are found or expected ones are missing from the examples directory.",
    "examples:html:about": "npm run examples:about -s && echo In case of HTML examples, some API files are expected and can be generated by reading the HTML examples documentation.",
    "examples:html:gen-api-files": "bash ./scripts/build.sh gen-html-examples-api-files",
    "build": "npm run examples:html:gen-api-files && npm run test",
    "clean": "rm -rf examples/html_pages/api_files"
  },
  "devDependencies": {
    "jsdom": "^22.1.0",
    "mocha": "^10.2.0",
    "sinon": "^15.2.0"
  },
  "dependencies": {
    "jsupack": "^1.5.1"
  }
}
