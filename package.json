{
  "name": "nvc",
  "description": "Network View Controller",
  "scripts": {
    "test": "echo Running tests... && npm run test:core -s && npm run test:fsm -s && npm run test:quick -s && npm run test:zexpl -s && echo Finished!",
    "test:core": "mocha ./tests/nvc.js",
    "test:fsm": "mocha ./tests/nvc_fsm.js",
    "test:quick": "mocha ./tests/nvc_quick.js",
    "test:zexpl": "mocha ./tests/nvc_examples.js",
    "examples:html:about": "node -e \"console.log('Some tests may fail due to the absence of valid dependencies for HTML examples.\\nThis will certainly happen when the dependencies have never been generated for instance.\\nAs a result, it may be useful to read the documentation of HTML examples to generate the dependencies.')\"",
    "examples:html:gen-deps": "bash ./scripts/build.sh gen-html-examples-deps",
    "build": "npm run examples:html:gen-deps && npm run test",
    "clean": "rm -rf examples/html_pages/api_deps"
  },
  "devDependencies": {
    "jsdom": "^22.1.0",
    "mocha": "^10.2.0",
    "sinon": "^15.2.0"
  },
  "dependencies": {
    "jsupack": "^1.5.1"
  }
}
