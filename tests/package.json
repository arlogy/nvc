{
  "name": "nvc_tests",
  "description": "npm package to test nvc locally",
  "scripts": {
    "test": "echo Running tests... && npm run test:core -s && npm run test:fsm -s && npm run test:quick -s && npm run test:zexpl -s && echo Finished!",
    "test:core": "mocha ./nvc.js",
    "test:fsm": "mocha ./nvc_fsm.js",
    "test:quick": "mocha ./nvc_quick.js",
    "test:zexpl": "mocha ./nvc_examples.js"
  },
  "devDependencies": {
    "jsdom": "^20.0.0",
    "mocha": "^10.0.0",
    "sinon": "^14.0.0"
  }
}
