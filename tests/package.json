{
  "name": "nvc_tests",
  "description": "npm package to test nvc locally",
  "scripts": {
    "test": "echo Running tests... && npm run test:core -s && npm run test:fsm -s && echo Finished!",
    "test:core": "mocha ./nvc.js",
    "test:fsm": "mocha ./nvc_fsm.js"
  },
  "devDependencies": {
    "jsdom": "^20.0.0",
    "mocha": "^10.0.0",
    "sinon": "^14.0.0"
  }
}
