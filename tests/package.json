{
  "name": "nvc_tests",
  "description": "npm package to test nvc locally",
  "scripts": {
    "test": "echo Running tests... && mocha ./nvc*.js && echo Finished!"
  },
  "devDependencies": {
    "jsdom": "^20.0.0",
    "mocha": "^10.0.0",
    "sinon": "^14.0.0"
  }
}
